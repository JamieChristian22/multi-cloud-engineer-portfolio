# Architecture - Enterprise Web Platform

A public-facing enterprise web platform deployed across cloud providers. The reference design uses a public web tier, hardened ingress rules, Linux web servers, and validation through browser and terminal evidence.

## Components

- Public web VM or compute instance
- HTTP/HTTPS ingress rules
- Linux OS baseline
- Nginx or Apache web server
- Basic operational monitoring
- Cost-aware cleanup runbook
