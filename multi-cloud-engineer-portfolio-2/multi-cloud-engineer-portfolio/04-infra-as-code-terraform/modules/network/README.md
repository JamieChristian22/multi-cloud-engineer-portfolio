# Terraform Network Module

This module creates a basic AWS VPC with public and private subnets, route tables,
and an internet gateway. It is intended as a starting point for landing zone designs
and can be composed with additional modules for security, compute, and data services.
