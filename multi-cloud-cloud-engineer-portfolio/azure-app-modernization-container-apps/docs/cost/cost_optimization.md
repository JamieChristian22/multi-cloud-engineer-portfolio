# Azure Cost Optimization

- Container Apps scale rules can reduce idle cost vs always-on VMs
- Log Analytics retention capped at 30 days to control ingestion/retention costs
- Postgres SKU set to a small burstable tier for dev; right-size using metrics after 7 days
