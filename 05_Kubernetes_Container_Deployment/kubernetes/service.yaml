apiVersion: v1
kind: Service
metadata:
  name: cloud-insurance-service
  namespace: cloud-portfolio

spec:
  selector:
    app: cloud-insurance-app

  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080

  type: LoadBalancer